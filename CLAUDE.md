# jinwookshin.vercel.app

채용 담당자와 그들의 에이전트가 읽는 사이트. 목표 반응: 만들 줄 알고, 판단을 남긴다.

선택의 이유는 `docs/decisions.md`에 있다. 여기는 규칙만 쓴다.

## 철학

브루탈리즘을 외형이 아니라 원리로 가져온다. 재료의 정직, 구조의 노출.
계보는 스위스 그리드, gwern.net, SSENSE 2017 전환(Eric Hu · Matthew Tsang).
neo-brutalism과 anti-design은 이 프로젝트의 방향이 아니다.

1. 시맨틱 HTML이 곧 레이아웃
2. 색은 작업물만 갖는다
3. JS 없이도 전문이 읽힌다
4. 표층은 짧게, 근거는 펼쳐서
5. 사람과 에이전트 둘 다 읽는다

## 토큰

단일 출처는 `app/globals.css`의 `@theme`. 다른 파일에 값을 복제하지 않는다.

- radius 0
- 잉크는 `#333` 계열. 순검정은 아껴 쓴다
- 회색 5~7개, 링크색 1개. 브랜드 컬러 CTA 없음
- 크기는 `clamp()`와 `ch`로. 본문 65ch

## 서체

- 디스플레이: 시스템 세리프. 페이지당 한 번, 트래킹 −0.03em
- 본문과 UI: Pretendard. 라벨은 11px 대문자, 자간 0.1em
- 데이터와 코드: IBM Plex Mono

큰 글자와 작은 글자만 두고 중간을 없앤다.

## shadcn

Radix의 접근성만 가져온다.

- 쓸 것: Popover, Dialog, Collapsible, Tooltip
- 쓰지 말 것: Card, Badge, 기본 Button variants, 기본 테마
- 설치 직후 radius 0, shadow 제거, 토큰 재작성
- 필요할 때 한 개씩. 미리 깔지 않는다

## 금지

그라데이션. box-shadow. 둥근 모서리. 이모지. 중앙 정렬 히어로. 스크롤 애니메이션.
CSS-in-JS. 아이콘 라이브러리 통째 설치. 클라이언트에서 콘텐츠 fetch.
컴포넌트에 텍스트 하드코딩(콘텐츠는 `content/**/*.mdx`).

## 예산

페이지 100KB · LCP 1.5s · CLS 0 · 런타임 의존성 10개. 넘으면 기능을 뺀다.

## 에이전트 표면

`/llms.txt` · `/[route].md` + Copy as Markdown · `Accept: text/markdown` 협상 · `/okf/` 번들.

SEO 대책이 아니다. Google은 Search에 불필요하다고 밝혔다.
AI 애플리케이션 엔지니어라는 주장을 사이트가 증명하는 장치로만 정당화된다.

## 작업 방식

- 배포부터 하고 고친다. 완성 후 공개가 아니다
- 가설에 충실도를 맞춘다. 조판은 HTML 한 장, 정보 구조는 종이, 인터랙션만 코드
- 잇지 않을 점을 고른다. 피드백 전부 반영은 목표가 아니다
- 파일 3개를 넘기면 계획을 먼저 보인다
- 의존성 추가는 예산 문제다. 넣기 전에 묻는다
- 커밋은 작게. 한 커밋에 한 결정

## 커밋

[Conventional Commits 1.0.0](https://www.conventionalcommits.org/ko/v1.0.0/)을 따른다.

```
<type>(<scope>): <subject>

<body>

<footer>
```

- `type`: feat, fix, docs, style, refactor, perf, test, build, ci, chore, revert
- `scope`: 선택. 바뀐 영역을 적는다 (tokens, content, agents, config)
- `subject`: 한국어 명사형, 72자 이내, 마침표 없음
- `body`: 무엇을 바꿨는지가 아니라 왜 바꿨는지. 72자에서 줄바꿈
- `footer`: `BREAKING CHANGE:`, 이슈 번호

한 커밋에 한 결정. 문서와 코드가 함께 바뀌면 결정 단위로 나눈다.

## 글쓰기

문서도 산출물이다. AI 냄새는 단어가 아니라 구조에서 난다.
길어지면 문장을 다듬지 말고 뼈대를 다시 짠다.

참조 문서(이 파일, README)는 소제목과 불릿을 쓴다. 규칙은 목록이 맞다.

- 결론을 먼저 쓴다
- 한 문서에 한 가지 목적. 소개와 규칙과 이유를 섞지 않는다
- 불릿은 조각으로. 완결 문장이 이어지면 문단으로 쓴다
- 볼드는 문서당 다섯 개 안쪽
- 표는 격자 데이터일 때만
- "A가 아니라 B다" 구문을 반복하지 않는다

산문(about 페이지, 프로젝트 서술, 블로그 글)은 규칙이 다르다.

- 주제의 중요성이나 배경을 설명하는 서론을 쓰지 않는다.
  이 글에서만 할 수 있는 판단이나 사례로 연다
- 시작점은 종류를 따른다. 주장은 판단부터, 경험은 장면부터, 분석은 사례부터
- 소제목, 번호, 불릿을 쓰지 않는다
- 앞 문단의 핵심어를 다음 문단이 이어받는다.
  '또한, 한편, 따라서'로 문단 관계를 설명하지 않는다
- 마지막에 요약이나 교훈을 붙이지 않는다.
  핵심 판단이 담긴 문장에서 끝내고 그 뜻을 다시 풀지 않는다

초안이 서-본-결로 나오면 `/restructure`로 뼈대를 다시 짠다.
문체가 어색하면 그 다음에 손본다. 구조가 먼저다.
